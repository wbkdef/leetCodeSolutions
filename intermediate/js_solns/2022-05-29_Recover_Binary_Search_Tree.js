// https://leetcode.com/problems/recover-binary-search-tree/


// Definition for a binary tree node.
function TreeNode(val, left, right) {
    this.val = (val===undefined ? 0 : val)
    this.left = (left===undefined ? null : left)
    this.right = (right===undefined ? null : right)
}

/**
 * @param {TreeNode} root
 * @return {void} Do not return anything, modify root in-place instead.
 */

function getMinMax(root) {
    if root === undefined
    if root.left
};

function findFirstNodeThatsTooBig(root, parent) {
    // Returns fnttb, fnttb_parent, min_val, max_val

    console.log("Hello world from findFirstNodeThatsTooBig")
};

function recoverTree(root) {
    console.log("Hello world")
    let fnttb, fnttb_parent = findFirstNodeThatsTooBig(root)
};

recoverTree(TreeNode(5))
